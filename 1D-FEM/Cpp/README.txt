This code works for inhomogeneous Dirichlet Boundary Conditions.

Specific Poisson Problem solved is::
    a.) -u'' = f
    u(0) = 1;
    u(pi) = -1;
    f = cos(x);

    u_exact = cos(x);
    u_h = {...};